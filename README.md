# flask-users-crud
# Flask Users CRUD App

This is a simple Flask web application that allows you to:
- View all users
- Add a new user
- Edit or delete users

## Requirements
- Python 3.x
- Flask

## How to Run
```bash
source venv/bin/activate
python app.py --host=0.0.0.0
